/**
 * Application entry point.
 * TODO: mount UI, initialize PairingSession, wire up SerialTransport.
 */
const app = document.getElementById("app");
if (app) {
  app.textContent = "DroneLink — initializing…";
}
