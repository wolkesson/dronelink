{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://dronelink.local/protocol/schemas/video-control-state.schema.json",
  "title": "Video Control State",
  "description": "Air-to-ground acknowledgment of an applied video control, forwarded to any connected GUI viewer. The `control` field discriminates the payload shape; only \"quality\" is defined today, mirroring video-control-request.schema.json.",
  "type": "object",
  "oneOf": [
    {
      "additionalProperties": false,
      "required": ["type", "control", "preset", "videoActive"],
      "properties": {
        "type": { "const": "video-control-state" },
        "control": { "const": "quality" },
        "preset": { "enum": ["auto", "high", "low", "data-only"] },
        "videoActive": { "type": "boolean" }
      }
    }
  ]
}
