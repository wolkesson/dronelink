{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://dronelink.local/protocol/schemas/video-control-request.schema.json",
  "title": "Video Control Request",
  "description": "Ground-to-air request to change a video setting. The `control` field discriminates the payload shape; only \"quality\" is defined today. Future controls (e.g. camera-source, zoom, gain, contrast) are expected to be added as additional oneOf branches, not by renaming this message.",
  "type": "object",
  "oneOf": [
    {
      "additionalProperties": false,
      "required": ["type", "control", "preset"],
      "properties": {
        "type": { "const": "video-control-request" },
        "control": { "const": "quality" },
        "preset": { "enum": ["auto", "high", "low", "data-only"] }
      }
    }
  ]
}
